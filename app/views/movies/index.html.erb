<p id="notice"><%= notice %></p>

<h1>Movies</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Description</th>
      <th>Duration</th>
      <th>Year</th>
      <th>Director</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
  <tr id="new_movie_form" > 
  <%= render 'form', movie: @movies.new %> </tr>
    <% @movies.each do |movie| %>
      <tr id="<%= dom_id(movie) %>">
        
        <td id="<%= dom_id(movie)%>_title">
        
          <%= link_to movie.title, 
            edit_title_url(id: movie.id, movie: "movies"), 
            remote: true do %> 
            </td>
          <% end %>
          <td><%= movie.description %></td>
          <td><%= movie.duration %></td>
          <td><%= movie.year %></td>
          <td><%= movie.director.name %></td>
          <td><%= link_to 'Destroy', movie, method: :delete,
          remote: true%> </td>
      </tr>
    
    
    <% end %>
  </tbody>
</table>

<br>

